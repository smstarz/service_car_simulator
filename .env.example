# Example environment variables for Service Car Simulator
# Copy to .env and edit values as needed

# Server port
PORT=5000

# TMAP and MAPBOX keys (if used)
TMAP_API_KEY=
MAPBOX_TOKEN=

# NODE_ENV
NODE_ENV=production
# Copy to .env and put your Mapbox token here
TMAP_API_KEY=your_tmap_api_key_here
MAPBOX_TOKEN=pk.your_mapbox_token_here
PORT=5000
